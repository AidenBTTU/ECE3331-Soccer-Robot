This repository contains all the code we used to control the Red Raider Soccer Robot. 
The code is structured as a state machine, where the robot operates between different defined states that require different types of data
to be processed. State transitions occur at key moments, such as when a ball is detected or when a ball is scored. 
